<template>
  <div class="item">
    <div class="item-img">
      <lazy-image src="{{item.picUrl?fileUrl+item.picUrl:url}}" width="50%" mode="aspectFill"/>
    </div>
    <div class="item-info">
      <div class="item-row row">
        <div class="item-col">
          <div class="item-label">姓名/</div>
        </div>
        <div class="item-col">
          <div class="item-value name">{{item.agentName}}</div>
        </div>
      </div>
      <div class="item-row row">
        <div class="item-col">
          <div class="item-label">电话/</div>
        </div>
        <div class="item-col">
          <div class="item-value phone">{{item.phone}}</div>
        </div>
      </div>
      <div class="item-row row">
        <div class="item-col">
          <div class="item-icon iconfont icon-phone" @tap.stop="onHandlePhoneAgent"></div>
        </div>
        <div class="item-col">
          <div class="item-icon iconfont icon-update" @tap.stop="onHandleUpdateAgent"></div>
        </div>
        <div class="item-col">
          <div class="item-icon iconfont icon-delete" @tap.stop="onHandleDeleteAgent"></div>
        </div>
      </div>
    </div>
  </div>
</template>

<script type="text/ecmascript-6">
  import wepy from '@wepy/core';
  import * as $routes from '../../router';
  import * as $config from '../../config';
  import apis from '../../apis';

  wepy.component({
    data: {
      fileUrl: apis.fileUrl,
      url: $config.DEFAULT_IMAGE
    },
    props: {
      item: {
        type: Object,
        default: {}
      }
    },
    events: {},
    watch: {},
    methods: {
      onHandlePhoneAgent() {
        const {phone} = this.item;
        wx.makePhoneCall({
          phoneNumber: phone,
          success: (res) => {
            console.log(res);
          },
          fail: (err) => {
            console.log(err);
          }
        });
      },
      onHandleDeleteAgent() {
        const {id} = this.item;
        this.$parent.onHandleDeleteAgent({id});
      },
      onHandleUpdateAgent() {
        const query = '?item=' + JSON.stringify(this.item);
        wx.navigateTo({
          url: $routes.AGENT.UPDATE.path + query
        });
      },
      onHandleDetailAgent(id) {
      }
    },
    created() {
    }
  });
</script>

<style scoped lang="less">
  @import "../../assets/less/common";
  @import "../../assets/less/variable";

  .item {
    overflow: hidden;
    background-color: @white;
    border-radius: @borderRadius20;
    .item-img {
      height: unit(240, rpx);
      image {
        border-top-left-radius: @borderRadius20;
        border-top-right-radius: @borderRadius20;
      }
    }
    .item-info {
      position: relative;
      padding: unit(20, rpx);
      line-height: unit(40, rpx);
      .item-row {
        margin-bottom: unit(5, rpx);
        .item-col {
          .item-label {
            font-size: @fontSize28;
            font-weight: bold;
          }
          .item-value {
            font-size: @fontSize28;
            &.name {
              font-weight: bold;
              color: @fontColor1;
            }
            &.phone {
              font-size: @fontSize28;
              color: @fontColor3;
            }
          }
          .item-icon {
            width: unit(60, rpx);
            height: unit(60, rpx);
            line-height: unit(60, rpx);
            font-size: @fontSize36;
            text-align: center;
            &.icon-phone {
              color: @stateColor1;
            }
            &.icon-update {
              color: @btnUpdate;
            }
            &.icon-delete {
              color: @btnDelete;
            }
          }
        }
        &:last-child {
          margin: 0;
          position: absolute;
          top: unit(-60, rpx);
          right: 0;
        }
      }
    }
  }
</style>

<config>
  {
  usingComponents: {
  'lazy-image': '../lazy-image/lazy-image'
  }
  }
</config>
